services:
  - type: web
    name: ttb-label-pre-screener
    runtime: docker
    plan: starter
    healthCheckPath: /healthz
    autoDeploy: true
    # Render uses the Dockerfile CMD, including its single Uvicorn worker.
